The title wrongly calls a nearby misspelling a “filename” when `/` only has folders, the body only asks “is it spelled correctly?” without showing `ls`/`cd` plus the name, and the decorators fail again: generic `target_near_sibling` with no file/dir split, plus unearned omitted `cwd_has_children`, `cwd_is_root`, and impossible `target_in_parent`.
