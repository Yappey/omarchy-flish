The hint is false in `/` because it calls nearby items “files” when only folders exist, gives no real command (`ls`/`cd` plus the name), and repeats the same decorator problems: generic `target_near_sibling` instead of a file/dir split, plus unearned omitted `cwd_has_children`, `cwd_is_root`, and impossible `target_in_parent`.
