The folder-vs-file idea is sound, but empty `requires` lets it fire on empty `rocks/` as well as `lighthouse/`, implying files inside and tempting `cat logbook.txt` from `/` with no `cd` or path shown, while the listed decorators go unused and the needed “has files” gate is missing.
